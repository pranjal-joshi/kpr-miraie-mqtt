services:
  miraie-bridge:
    build: .
    container_name: miraie-bridge
    restart: unless-stopped
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./devices.yaml:/app/devices.yaml:ro
